# orchestrate run r-muchbgqz-v7d6oz

- mode: supervisor | agent: session-40bbfeca-c32a-4b04-b686-f3b4592d4797 | provider: spawn
- concurrency: 3 | durationMs: 389568 | aborted: false
- startedAt: 2026-09-22T09:36:02.699Z | finishedAt: 2026-09-22T09:42:32.267Z

## Challenge adversarial du lot T306 phase 1 [completed]

[structured] {"verdict":"NUANCE","hash_revision":"Lot NON COMMITTE. Parent annoncé 0358c631 (T383 committé). HEAD effective au moment du contrôle : 9d6f99a9 (2 commits doc/contrat après 0358c631, n'affectant pas les 5 fichiers du lot - le diff du lot est identique). hash-object du contenu NON COMMITTE sur working tree : GVL_Simulation : 30e376fc0fc7c1b7439d1c1b1a1cf33edea224ef · FB_Sim_Safety : b102b0dc27e72b00fe581196fd2be3f2e4a9637f · FB_SimBench : 8c94038bbb6e772e1309f79f70bafc63be9e5d3a · PRG_02_Acquisition : 2573f446de8f458ce7bf3cd99a92f0daa1cb4f1e · TOOLS/TEST_AUTO_CI/RESULTS/L_SIMULATION/tests/test_fb_sim_safety.st : c294737b403b84dc1b75745f92700cf699eebef9. Contrôles rejoués : run_tests.py --fb FB_Sim_Safety = 3/3 PASS ; G200 --report = PASS 0 erreur (138 OK L1-7, 2052 instances) ; périmètre interdit (B_AU_SECURITE/DOC-AF/STDS/PRJ_CODESYS) = vide.","constats":[{"fichier":"CODE/M_MAIN/PRG_02_Acquisition.st","gravite":"INFO","ligne":"138+289+448; FB_SimBench.st:258-269; :190","point":"Q1 - Inertie réelle du banc en production : double garde indépendante, le stimulus ne peut PAS atteindre HwIn.Machine.PhaseRotationOk_DI hors simulation","preuve":"PRG_02:289 Enable := GVL_Simulation.SimulationModeActive ; FB_SimBench:258 'IF NOT Enable THEN ... Machine := HwReal.Machine; RETURN' => le bloc §5 (ou l.692 Machine.PhaseRotationOk_DI := instSimSafety.SimPhaseRotationOk) n'est exécuté que si SimulationModeActive. Même si le banc s'exécutait, PRG_02:138 MachineInputSourceSimulated := SimulationModeActive AND SimSafetyActive et :448 HwIn.Machine := SEL(MachineInputSourceSimulated, HwReal.Machine, HwSim.Machine) sélectionnent HwReal en prod (:190 HwReal.Machine.PhaseRotationOk_DI := PhaseRotationOk_DI, source GVL_Device_IO:53). Aucun autre chemin : tous les consommateurs (PRG_07:407/:429, PRG_04:945/:1015, PRG_05:479) lisent HwIn.Machine.PhaseRotationOk_DI, seul producteur/arbitre = SEL :448. VERIFIE. Scénarios 'Enable forcé'/'autre chemin d'ecriture' REFUTES : forcer Enable sans SimulationModeActive+SimSafetyActive ne bascule jamais l'aiguillage.","verdict":"CONFIRME"},{"fichier":"TOOLS/TEST_AUTO_CI/RESULTS/L_SIMULATION/tests/test_fb_sim_safety.st","gravite":"MAJOR","ligne":"43-62","point":"Q2 - La preuve par mutation (AC5) est quasi TAUTOLOGIQUE : le test ne prouve que la traversée feuille de FB_Sim_Safety, pas la chaîne d'injection, ni AC2","preuve":"TC-T306-SAF-003 instancie FB_Sim_Safety seul, force Enable:=TRUE, lit SimPhaseRotationOkBit, et ASSERT fb.SimPhaseRotationOk. Il contourne GVL_Simulation, PRG_02:337, FB_SimBench (VAR_INPUT+appel instSimSafety+:692), et SEL :448. 'Mutation propagation neutralisée' => FAIL parce que le test lit EXACTEMENT la sortie du code muté : dépendance directe/circulaire, pas une mutation forte. Le test passerait même si le câblage GVL->PRG_02->FB_SimBench->HwIn était cassé (tant que FB_Sim_Safety propage). AC2 ('Forcer le stimulus à FALSE rend PhaseRotationOk_DI = FALSE dans l'image machine simulée') n'est donc PAS prouvé par un test, seulement DEDUIT de la cohérence du câblage + G200. De plus le commentaire du test ligne 54 '(PhaseRotationOk_DI tombe)' est trompeur : l'assertion porte sur fb.SimPhaseRotationOk (sortie FB), jamais sur PhaseRotationOk_DI.","verdict":"CONFIRME"},{"fichier":"DOC/WFLOW/REX/NOTE_APPLICATION_T306_PHASE1_INJECTION_SIM_20260922.md","gravite":"MINOR","ligne":"27-44","point":"Q2bis - L'injectabilité de bout en bout (GVL->HwIn.Machine.PhaseRotationOk_DI) n'est pas 'vérifiée' par exécution, elle est seulement déduite","preuve":"La note classe le lot 'injectable et vérifié' alors que la seule exécution (ROUGE/VERT §3.2) est le test feuille de FB_Sim_Safety. Le vrai chainon GVL->PRG_02->FB_SimBench:692->HwSim->SEL->HwIn n'a aucun test d'intégration qui force GVL_Simulation.SimPhaseRotationOk=FALSE et observe HwIn.Machine.PhaseRotationOk_DI. Rejoué sur les gates : le test ne couvre que le FB. Il manque un test d'intégration (type test_main_end_to_end) pour matérialiser l'AC2.","verdict":"NUANCE"},{"fichier":"CODE/L_SIMULATION/FB_Sim_Safety.st","gravite":"INFO","ligne":"156-162","point":"Q3 - Design : point d'injection via FB_Sim_Safety cohérent avec le patron SimChainOkBit->SimChainOk, et polarité SimPhaseRotationOk (TRUE=sain) conforme au fail-safe de PhaseRotationOk_DI","preuve":"FB_Sim_Safety agrège bien les sorties sécurité sim (SimChainOk, SimContactorOk, SimPhaseRotationOk) : passer le stimulus par ce FB (plutôt qu'une écriture directe dans FB_SimBench) est la bonne couche. Polarité : GVL_Simulation.st:34 SimPhaseRotationOk : BOOL := TRUE, et FB_Sim_Safety.st:162 SimPhaseRotationOk := SimPhaseRotationOkBit, cohérent avec PhaseRotationOk_DI fail-safe TRUE=OK (FB_Safety_Translation.st:22). Pas de double implémentation (nom canonique seul).","verdict":"CONFIRME"},{"fichier":"CODE/L_SIMULATION/FB_Sim_Safety.st","gravite":"MINOR","ligne":"79-82","point":"Q3bis - Biais de polarité sur branche NOT Enable : FB_Sim_Safety force SimPhaseRotationOk := FALSE quand Enable=FALSE, ce qui pour un 'stimulus' revient à 'injecter un défaut à la désactivation' ; dormant aujourd'hui mais incohérent avec la sémantique TRUE=sain","preuve":"Dans la branche 'IF NOT Enable THEN' :82 SimPhaseRotationOk := FALSE (comme SimChainOk/SimContactorOk qui sont des sorties actives-safe où FALSE=inactif a du sens). Pour un stimulus pur passthrough, forcer FALSE hors Enable est sémantiquement inverse. Sans impact prod (instSimSafety Enable=TRUE codé en dur dans FB_SimBench:681, lui-même sous la garde Enable de FB_SimBench:258, et l'image machine n'est publiée que sous SimulationModeActive AND SimSafetyActive), mais c'est un coin dormant à documenter. NB : SimPhaseRotationOk n'est pas remis à sa valeur par défaut dans le bloc de sortie de simulation PRG_02:96-121 (contrairement aux autres stimuli) ; la valeur stale ne rejoint pas HwIn en sortie de sim (SimSafetyActive:=FALSE), mais elle PERSISTE à la ré-entrée du mode sim.","verdict":"NUANCE"},{"fichier":"CODE/L_SIMULATION/FB_SimBench.st","gravite":"INFO","ligne":"64+678-692","point":"Q4 - Aucune régression / effet de bord : producteur unique conservé, SimSafetyActive intact, encapsulation_check FB_SimBench (0 lecture GVL) préservé","preuve":"SimPhaseRotationOk n'a qu'un seul consommateur (PRG_02:337) et aucune lecture GVL ajoutée dans FB_SimBench (seulement un VAR_INPUT câblé par PRG_02) -> gate encapsulation (0 lecture GVL) maintenu. SimSafetyActive/SimulationModeActive inchangés (PRG_02:89-121). Tests pré-existants TC-P13-SAF-001/002 rejoués = PASS identiques (3/3, 0 FAIL). G200 PASS et périmètre interdit vide. Aucun autre consommateur du bit nouveau n'est interféré.","verdict":"REFUTE"},{"fichier":"DOC/WFLOW/CONTRACTS/TASK_CONTRACT_T306_PHASE1_INJECTION_SIM.yaml","gravite":"MAJOR","ligne":"14-32 (AC2/AC5); BRIEF §4 phase 1","point":"Q5 - Trous de preuve : l'objectif métier T306 (arrêt M1/M2/M3 via la chaîne de sécurité) reste NON PROUVÉ, et l'AC2 end-to-end non testé","preuve":"Aucun test ne prouve : (1) que forcer GVL_Simulation.SimPhaseRotationOk=FALSE met bien HwIn.Machine.PhaseRotationOk_DI à FALSE (AC2 - uniquement DEDUIT du câblage) ; (2) que le défaut atteint les 3 axes : FB_Safety_Winch (M1/M2) cause 4 bit 16#0010 et FB_Safety_Translation (M3) cause 2 bit 16#0004, avec SafeStop, permis annulés, freins serrés, RelayFwd/Rev+Contactor+BrakeCmd=FALSE ; (3) pas de réarmement auto (latch SET seul, Reset sur front, re-verrouillage si cause persistante). La note §6 le reconnaît ('Essai machine phase 3', 'les 2 correctifs de sécurité restent hors phase 1'). Le lot prouve seulement la plomberie d'injection (feuille), pas l'effet sécurité. Tests manquants à nommer : un test d'intégration PRG_02 end-to-end (GVL.SimPhaseRotationOk=FALSE -> HwIn.Machine.PhaseRotationOk_DI=FALSE), un test de réaction 3 axes (SafeStop/latch/interlocks/freins), un test latch/Reset front sans réarmement auto.","verdict":"CONFIRME"}],"indetermines":["Le câblage end-to-end GVL_Simulation.SimPhaseRotationOk=FALSE -> HwIn.Machine.PhaseRotationOk_DI=FALSE n'est pas vérifié par exécution d'un test d'intégration : DEDUIT de la cohérence du câblage (PRG_02:337->FB_SimBench VAR_INPUT->instSimSafety->:692->HwSim->SEL:448) + G200 PASS, non VERIFIE par un test qui force la GVL et observe HwIn.","La réaction des 3 axes au défaut injecté (SafeStop, permis nuls, freins, contacteurs/relais FALSE) n'est prouvée par aucun test CI.","Verrouillage : absence de réarmement auto + Reset sur front + re-verrouillage si cause persistante, suite au défaut injecté, non testé (réservé phase 3 machine / lot C4 séparé).","Comportement à la ré-entrée du mode simulation si SimPhaseRotationOk a été laissé à FALSE en sortie (non reset par le bloc PRG_02:96-121) : non déterminé par test.","Scénario hors contrat mais relevant du devoir d'alerte : si un opérateur force SimulationModeActive=TRUE sur machine de production, tout le domaine Machine sim est basculé (risque préexistant, commun à tous les stimuli, pas introduit par ce lot)."]}
Preuve confirmée des deux côtés : le test `FB_Sim_Safety` repasse 3/3 (le cas additif `TC-T306-SAF-003` est vert), `G200_check_linkage.py` PASS (0 erreur), périmètre interdit (B_AU_SECURITE/DOC-AF/STDS/PRJ_CODESYS) vide. Je consolide l'analyse contradictoire.

J'ai tout ce qu'il faut. Voici ma conclusion de challenge.

## Expertise securite : SafeStop M3 + bypass MAINT_N2 [completed]

[structured] {"verdict":"BLOCK — 2 écarts sécurité confirmés (proposés C4 chacun), correctifs à livrer en UN lot de sécurité T306 phase 2 (contrat + double revue exigés) avant toute mise en prod. ÉCART A : SafeStop inerte à la barrière finale M3 (perte de défense en profondeur). ÉCART B : Bypass PhaseRotation non gaté MAINT_N2 sur 3 axes + masquage croisé par BypassProcess — à GATER (ne pas laisser) : la 'dérogation MES 2026' est RÉELLE mais temporaire, [NON TESTE], et explicitement non conforme ISO 13849 ; elle ne justifie pas de neutraliser en production la protection rotation de phases.","hash_revision":"9d6f99a99fb0ec00e18f36f0f22b2bb5534e8111","constats":[{"fichier":"CODE/I_TRANSLATION/FB_TranslationOutputInterlock.st","gravite":"BLOCK","ligne":"15 (décl., seul input non lu) ; corps 69-156","point":"ÉCART A — proposé C4. SafeStop déclaré en VAR_INPUT (FB_TranslationOutputInterlock.st:15) mais JAMAIS lu dans le corps (l.69-156) : le FB ne couple ni DriveControlWord ni BrakeCmd sur SafeStop. La chaîne est pourtant câblée (fb_Safety rot.phase → M3_SafeStop_Aggregate → TranslationFinalInterlockRequest.SafeStop PRG_05:674 → PRG_06_Outputs.st:438). M3 repose donc sur UN SEUL mécanisme d'arrêt (rampe FB_Translation:270) sans coupure dure finale indépendante — perte de défense en profondeur sur un axe porteur de charge (personnes exposées). Contraste FB_WinchOutputInterlock.st:16 consommé :178/:335/:340/:393.","preuve":"grep 'SafeStop\\s+:' CODE/ → 6 FB ; 5 lus, 1 inerte. PRG_06_Outputs.st:438 'SafeStop := PRG_05_Translation.Data.TranslationFinalInterlockRequest.SafeStop' câble le signal, FB ne le consomme pas."},{"fichier":"CODE/M_MAIN/PRG_04_Treuils_Benne.st","gravite":"BLOCK","ligne":"984 (M1), 1050 (M2), PRG_05_Translation.st:496 (M3) ; gate témoin PRG_05:500","point":"ÉCART B — proposé C4. Bypass PhaseRotation non gaté MAINT_N2 sur les 3 axes : PRG_04:984 (M1) & :1050 (M2) câblé brut sur GVL_IHM (commentaire :970 'SANS gate de mode (dérogation MES 2026, tous modes)'), PRG_05:496 (M3) non gaté alors que :500 gate le voisin BypassLimitSwitch avec M3_MaintN2 (:462). → un défaut de rotation de phases (inversion/perte ⇒ sens/vitesse erronés ⇒ charge non maîtrisée) peut être neutralisé en mode PRODUCTION (SEMI_AUTO/AUTO). Violation du principe ISO 13849 « bypass en mode maintenance contrôlé » et de la doctrine ST_BypassWinch.st:7.","preuve":"BypassPhaseRotation := GVL_IHM.<axe>.Bypass.PhaseRotation (brut). PRG_05:500 'BypassLimitSwitch := M3_MaintN2 AND (...)' gate le voisin ; M3_MaintN2 défini :462 = (Auth.Mode = E_Mode.MAINT_N2)."},{"fichier":"CODE/H_TREUILS_BENNE/FB_Safety_Winch.st","gravite":"MAJOR","ligne":"302 ; FB_Safety_Translation.st:140","point":"Complément ÉCART B — proposé C3. Gater le seul BypassPhaseRotation serait INCOMPLET et créerait une fausse assurance : la cause 4 (rot.phase) est neutralisée par 'NOT (BypassProcess OR BypassPhaseRotation)' → BypassProcess SEUL lève aussi la protection (et il inclut le bypass commun SlackCable, PRG_04:974/1042). Le lot doit désolidariser la cause rot.phase de BypassProcess pour restaurer une vraie neutralisation MAINT_N2-only.","preuve":"FB_Safety_Winch.st:302 'IF NOT (BypassProcess OR BypassPhaseRotation) AND NOT PhaseRotationOk THEN' ; FB_Safety_Translation.st:140 identique."},{"fichier":"DOC/AF/AF_Partie-05_Modes_Maintenance_v2.1.md","gravite":"INFO","ligne":"179-192","point":"Dérogation MES 2026 EXISTE sur disque (VÉRIFIÉ). Commit cac7387d (Mathieu Gros 2026-09-02, 'feat(safety): DEROGATION MES - bypass treuil M1/M2+benne sans gate de mode [NON TESTE]') → PRG_04 + AF_Partie-05. Documentée AF_Partie-05_Modes_Maintenance_v2.1.md:179-192 : 'écart assumé vis-à-vis de la Directive Machines / ISO 13849 … À revoir … une fois la MES stabilisée'. TEMPORAIRE, [NON TESTE], non conforme, à périmètre dérogé (bypass comm/position non-critiques). Le gate G483_check_bypass_matrix_mode_gated.py est ROUGE aujourd'hui. Ne justifie PAS de laisser PhaseRotation en production.","preuve":"git show cac7387d (4 fichiers : PRG_04 110 l., CODE_XML, AF-05 34 l.) ; extrait 'DÉROGATION MES septembre 2026 (décision opérateur, tracée)' + 'Colonnes N1/N2 périmées depuis la dérogation'."},{"fichier":"CODE/I_TRANSLATION/","gravite":"INFO","ligne":"(inventaire classe SafeStop non-lu)","point":"Recherche de première main des SafeStop déclarés-non-lus hors M3 : AUCUNE autre occurrence. Tous les inputs SafeStop sont consommés (FB_Translation:24→:270, FB_Winch:29→:169, FB_WinchOutputInterlock:16→:335/:340/:393) ; les autres SafeStop sont des SORTIES (FB_Safety_Winch:90, FB_Safety_Translation:56). FB_TranslationOutputInterlock:15 est l'unique input inerte de la classe de défaut ÉCART A.","preuve":"grep exhaustif 'SafeStop\\s+:' sur CODE/ → 6 FB ; 0 autre input-inerte découvert."},{"fichier":"CODE/I_TRANSLATION/FB_Safety_Translation.st","gravite":"MAJOR","ligne":"140-145 (cause 2 latchée → SafeStop :265)","point":"Risque physique concret (A et B partagent le même déclencheur) : défaut/inversion d'une phase ⇒ ordre de sens/rotor erroné ou non-décélération ⇒ mouvement non maîtrisé de la benne/treuils (charge de dragage lourde) ⇒ personne exposée sur le pont. Sur B, en production, le défaut est totalement masqué (aucun arrêt exigé). Sur A, l'arrêt sur défaut de phase repose sur un seul chemin logiciel sans coupure dure finale en cas de défaillance coïncidente de FB_Translation.","preuve":"Chaîne rot.phase M3 : cause 2 → SafeStop → rampe unique FB_Translation ; pas de PowerCutOff (masque 16#00F8, bit2 hors, :268)."}],"priorite":["1. Bloc SECURITE T306 — ÉCART B (C4) : gater BypassPhaseRotation sur MAINT_N2 des 3 axes ET neutraliser le masquage de la cause rot.phase par BypassProcess → restaurer la protection de phase en production.","2. Dans le même lot — ÉCART A (C4) : consommer SafeStop dans FB_TranslationOutputInterlock (BrakeCmd := FALSE + blocage du mouvement) pour recouvrer la défense en profondeur M3.","3. Validation (commune aux 2 écarts, même scénario) : bundle + G200_check_linkage PASS + run_all_gates (G483 redevenu vert) + test d'injection rot.phase phase 1 ROUGE avant/VERT après sur M1/M2/M3."],"decision_recommandee":"Un agent (DSH01), préparer UN lot UNIQUE 'T306 phase 2 correctifs sécurité' en C4 (contrat TASK_CONTRACT + double revue exigés), qui (a) dans CODE/I_TRANSLATION/FB_TranslationOutputInterlock.st au bloc de décision finale (§4, ≈:137 et :146) force BrakeCmd := FALSE et bloque le mouvement quand SafeStop=TRUE sans toucher à Reset/Enable/FaultCore/watchdog/PDO ; (b) gate sur MAINT_N2 BypassPhaseRotation sur PRG_04_Treuils_Benne.st:984/:1050 via (PRG_03_Modes_Cycle.Data.Auth.Mode = E_Mode.MAINT_N2) et sur PRG_05_Translation.st:496 via M3_MaintN2, et désolidarise la cause rot.phase de BypassProcess dans FB_Safety_Winch.st:302 / FB_Safety_Translation.st:140 ; puis exécuter le bundle complet + générer le diff bundle des fichiers touchés + G200_check_linkage.py --report et run_all_gates.py (G483 passé au vert) ; preuve attendue : G200 PASS (0 erreur), gates palier C verts, et test d'injection défaut rot.phase (préalable phase 1) VERT sur les 3 axes avec redondance finale vérifiée sur M3 et bypass inopérant hors MAINT_N2.","indetermines":["Niveau écran IHM du bypass PhaseRotation non auditable (aucun .vis hors CODE/) — la signalisation IHM du bypass est un complément non bloquant, pas le correctif, mais son absence rendait le bypass silencieux; confirmation IHM requise seulement si l'option 'laisser + signaler' était retenue (non retenue).","État de stabilité de la MES : la dérogation vise une phase de chantier non stabilisée ; confirmation exploitant que le besoin N1 dérogé porte bien des bypass non-critiques (comm/drive), distincts de PhaseRotation, avant de figer le périmètre dérogé restant.","Périmètre du correctif BypassProcess : son éventuel gating étendu touche le routage mou-de-cable conservé brut (PRG_04:508/:520 et SlackCable dans BypassProcess :974/:1042) — périmètre exact de la désolidarisation à confirmer par arbitrage humain (changement dans les FB de sécurité, zone sensible).","Aucun test CI M1/M2 d'injection rot.phase n'existe (PhaseRotationOk toujours TRUE dans les harnesses) ; la validation ROUGE/VERT des 3 axes exige l'injection de simulation préalable prévue par T306 phase 1 (GVL_Simulation.SimPhaseRotationOk), donc phase 1 bloquante avant de prouver le correctif par test."]}
## 🎬 BRIEFING SESSION — EXPERTISE SÉCURITÉ / AVIS DÉCISIONNEL (read-only)

**Briefing session T306 phase 2 — expertise sécurité par DSH01 (Agent: DSH01)** · Skills actives projet : `task-planner` (catalogue), `troubleshooting` (diagnostic).

**Périmètre** : 100 % lecture/git — aucune écriture, aucun commit. **HEAD vérifié** = `9d6f99a9`.

---

### ✅ Preuves de première main mesurées (pas de confiance dans le brief)

**ÉCART A — SafeStop inerte à la barrière finale M3**
- `FB_TranslationOutputInterlock.st:15` déclare `SafeStop : BOOL` en VAR_INPUT → **jamais référencé dans le corps (l.69-156)**. grep CODE : **1 seule occurrence**. Le FB ne couple ni `DriveControlWord` ni `BrakeCmd` sur SafeStop.
- Contraste : `FB_WinchOutputInterlock.st:16` → consommé `:178-181` / `:335` / `:340` / `:393` (coupure dure `RelayFwd/Rev` + contacteurs + frein) — défense en profondeur réelle M1/M2.
- La chaîne SafeStop M3 EST câblée : `FB_Safety_Translation.st:140` (cause 2 rot.phase) → `:265 SafeStop` → `M3_SafeStop_Aggregate` (`PRG_05:511-513`) → `TranslationFinalInterlockRequest.SafeStop` (`PRG_05:674`) → **câblé sur l'entrée `SafeStop` de l'interlock (`PRG_06_Outputs.st:438`) mais IGNORÉ**.
- Conséquence : sur défaut de phase M3, l'arrêt repose **uniquement** sur la rampe logicielle de `FB_Translation` (`:270` `EffectiveSafeStop`), sans coupure indépendante à la barrière finale.

**ÉCART B — Bypass `PhaseRotation` non gaté**
- `PRG_04_Treuils_Benne.st:984` (M1) / `:1050` (M2) : câblage brut `GVL_IHM.<axe>.Bypass.PhaseRotation`, commentaire `:970` *« SANS gate de mode (dérogation MES 2026, tous modes) »*.
- `PRG_05_Translation.st:496` (M3) non gaté, alors que `:500` gate son voisin : `BypassLimitSwitch := M3_MaintN2 AND (...)` (`M3_MaintN2` défini `:462`).
- **Masquage croisé** : la cause 4 rot.phase est neutralisée par `NOT (BypassProcess OR BypassPhaseRotation)` (`FB_Safety_Winch.st:302`, `FB_Safety_Translation.st:140`) → **`BypassProcess` seul lève aussi la protection** (et `BypassProcess` inclut le bypass commun `SlackCable`, `PRG_04:974/1042`). Un gate sur le seul `BypassPhaseRotation` serait **incomplet**.

**La « dérogation MES 2026 » EXISTE** ✅
- Commit `cac7387d` (Mathieu Gros, 2026-09-02) *« feat(safety): DEROGATION MES - bypass treuil M1/M2+benne sans gate de mode **[NON TESTE]** »* → `PRG_04` + `AF_Partie-05`.
- Documentée `AF_Partie-05_Modes_Maintenance_v2.1.md:179-192` : *« écart assumé vis-à-vis de la Directive Machines / ISO 13849 (neutralisation d'une protection normalement réservée à un mode maintenance contrôlé) … **À revoir** … une fois la MES stabilisée »*.
- **Nature** : TEMPORAIRE (phase de chantier MES), **[NON TESTE]**, explicitement non conforme, à périmètre dérogé (bypass comm/position non-critiques). **Ne justifie pas** qu'une neutralisation de la protection rotation-de-phases reste effective en production. Le gate `G483_check_bypass_matrix_mode_gated.py` est **ROUGE aujourd'hui** sur cette dérogation non réconciliée.

**Classe SafeStop déclaré-non-lu — unique**
- grep `SafeStop\s+:` sur CODE/ → 6 FB : `FB_Safety_Winch:90` (sortie), `FB_Safety_Translation:56` (sortie), `FB_Translation:24`→lu `:270`, `FB_Winch:29`→lu `:169`, `FB_WinchOutputInterlock:16`→lu. **Seul `FB_TranslationOutputInterlock:15` est un input non lu.** Aucune autre instance de la classe de défaut hors M3.

---

### 🩺 Verdict sécurité & décision tranchée

- **ÉCART A → proposé C4**, ÉCART B → **proposé C4** ; lot correctif **UNIQUE** (même scénario rot.phase, même validation d'injection, périmètre sécurité commun — les séparer dupliquerait la validation et pourrait livrer un écart non traité).
- **Priorité dans le lot : ÉCART B d'abord** (perte fonctionnelle directe de la protection en production), **puis ÉCART A** (redondance). Les deux restent un seul lot car le déclencheur et le test ROUGE/VERT sont identiques et déjà cadrés par T306 phase 1.
- **Décision ECART B : GATER** (sur `MAINT_N2`), **et lever le masquage par `BypassProcess`** sur la cause rot.phase. **Pas « laisser »** : la dérogation est temporaire, [NON TESTE], non conforme ISO 13849, et n'a été créée que pour les besoins N1 de chantier (bypass non-critiques).

**Risque physique concret (A & B)** : inversion/perte de phase ⇒ ordre sens/rotor erroné ou non-décélération ⇒ **mouvement non maîtrisé de la benne/treuils (charge de dragage lourde)** ⇒ personne exposée sur le pont. Sur B en production le défaut est **totalement masqué** (aucun arrêt exigé) ; sur A l'arrêt repose sur **un seul** mécanisme logiciel, sans coupure dure finale.

Enveloppes minimales :
- **A** : `FB_TranslationOutputInterlock.st` — §4 : `BrakeCmd := BrakeReleaseRequest AND NOT Fault.Error AND NOT RestartInhibit AND NOT SafeStop;` (≈:137) + `ELSIF MovementRequested AND NOT PermitFinalBlocked AND NOT SafeStop THEN` (≈:146). **Ne pas bouger** : Enable/PowerContactorEngaged, Reset/fronts, watchdog, RestartInhibit, FaultCore, échelle PDO.
- **B** : `PRG_04:984/:1050` → `...PhaseRotation AND (PRG_03_Modes_Cycle.Data.Auth.Mode = E_Mode.MAINT_N2)` (signaux déjà disponibles, cf. `:997`) ; `PRG_05:496` → `M3_MaintN2 AND ...` (miroir `:500`). **Ne pas bouger** : BypassProcess/SlackCable, routage mou-de-cable, RETAIN, logique IHM. **+ masquage croisé** : désolidariser la cause 4 de `BypassProcess` (`FB_Safety_*:302/:140`) — périmètre FB sécurité ⇒ C4, arbitrage humain + double revue.

---

Je rends l'avis décisionnel au format JSON demandé.

## Trancher les 2 questions de l'agent T306 [completed]

[structured] {"verdict":"Lot T306 phase 1 = IMPLEMENTATION CONFORME au contrat (diff du périmètre autorisé propre : +3/-1 FB_SimBench, +7 FB_Sim_Safety, +4 GVL, +2 PRG_02, +21 test additif pur ; zones forbidden vierges ; 0 suppression). NON CLOS tant que : (1) rejeu des preuves CI à l'état final, (2) commit sélectif validé humainement, (3) essai machine (recette, action HUM non délégable). Les 2 questions posées par l'agent T306 sont tranchées : pas de présentation hunk par hunk (bruit), et cadrage C4 MAINTENANT en parallèle (le regroupement de campagne porte sur les ESSAIS/import, pas sur le cadrage — gratuit pour l'exploitant). Décidé : commit du lot T306 immédiat, sélectif et isolé, avec marqueur [NON TESTE MACHINE], subordonné au GO humain (règle absolue AGENTS.md).","hash_revision":"9d6f99a9","decisions":[{"action":"Supprimer la demande HTH. L'orchestrateur lit directement `git diff -- CODE/L_SIMULATION/ CODE/M_MAIN/PRG_02_Acquisition.st TOOLS/TEST_AUTO_CI/RESULTS/L_SIMULATION/tests/test_fb_sim_safety.st` (preuve : mes commandes, numstat 3/7/4/2/21-0). Seule une CONFIRMATION UNE LIGNE sur les 3 lignes sensibles est utile si deja verifiee : (a) FB_SimBench:690 substitution TRUE→instSimSafety, (b) PRG_02:335 ligne de cablage unique, (c) FB_Sim_Safety sortie FALSE hors Enable (gated simulation). C'est deja couvert par la note REX §1/§2 — donc aucun round-trip necessaire.","justification":"Le diff reel du perimetre autorise est petit (5 fichiers, ~37 lignes nettes : FB_SimBench +3/-1, FB_Sim_Safety +7, GVL +4, PRG_02 +2, test +21) et a DEJA ete lu par l'orchestrateur (numstat + les 3 lignes sensibles). La regle orchestrator (SKILL.md R2-ligne 'git diff reel lu par l'orchestrateur, jamais a l'agent qui a produit le code') veut que la relecture se fasse sur le diff reel par l'orchestrateur lui-meme, pas sur une re-presentation de l'agent producteur (qui n'est pas une relecture independante). Le hunk par hunk n'ajoute aucune verification mecanique : pas de syntaxe nouvelle (3 patterns identiques au patron existant SimChainOkBit), pas de suppression, pas de zone forbidden touchee (verifie).","question":"Q1 — « Veux-tu que je te presente le diff reel hunk par hunk pour ta relecture ? »","tranche":"NON. Ne pas demander au lot une presentation hunk par hunk : c'est du bruit redondant."},{"action":"Lancer en parallele la redaction du contrat C4 (contrat TASK_CONTRACT_C4 + brief SafeStop M3 inerte + gate MAINT_N2 bypass) et l'enregistrer dans TASKS_ORCHESTRATOR.yaml, AVANT toute implementation. Regrouper les arbitrages en une seule salve (regle campagne) et preparer UN protocole d'essai unique pour la recette future regroupant T306 phase 1 + correctifs C4.","justification":"Le cadrage (contrat + brief) est du travail documentaire d'orchestrateur : zero cout de session humaine, donc hors de la regle de campagne qui groupe les ESSAIS machine/import, pas le cadrage. La gravite le justifie : SafeStop M3 inerte + gate MAINT_N2 bypass sont 2 ECARTS DE SECURITE confirmes en phase 0 (note REX §6) — plus on cadre tot, plus le correctif est pret pour la MEME campagne d'essais que T306. Le fait que T306 ne soit pas committe ne bloque pas le cadrage C4 ; il impose seulement de coordonner la collision d'ecriture PRG_02 (deja signalee au contrat T306 alert_duty) : un seul ecrivain par fichier a verifier AVANT implementation C4.","question":"Q2 — « Veux-tu que je prepare le lot C4 (SafeStop M3 + gate MAINT_N2) en vue de ton arbitrage ? »","tranche":"OUI. Cadrer le lot C4 MAINTENANT, en parallele, sans attendre la closure ni le commit de T306 phase 1."},{"action":"Preparer le commit selectif du lot uniquement (4 .st + test_fb_sim_safety.st + note REX T306 + artefacts CODE_XML du lot + rapports L_SIMULATION), soumettre le message et le contenu a validation humaine explicite, puis executer. Ne jamais `git add .` (risque d'emporter snapshot/runs/contrats d'autres lots).","justification":"Lot ferme sur le plan implementation : CI/gates palier C 21/21, G200 PASS 0 erreur, preuve ROUGE/VERT par mutation documentee (note REX §3.2), zones forbidden vierges, 0 suppression. Le precedent lot T383 a ete committe en 0358c631 avec marqueur [NON TESTE MACHINE] : le commit git precede la recette machine — la campagne ne concerne que les ESSAIS, pas les commits. Committer immediatement securise le travail (pattern 2 temps d'AGENTS.md : checkpoint/verrou avant de passer a C4) et reduit la surface de collision sur PRG_02 partage avec C4. Commit SELECTIF obligatoire : la working tree contient du bruit non lie (snapshots troubleshooting, contrats T364/T374, runs orchestrateur, CODE_XML d'autres lots) a NE PAS embarquer. Message propose conforme a la doctrine : `test(l_simulation): T306 phase1 - injection defaut rotation phases injectable - gates/G200 PASS, ROUGE/VERT prouve [NON TESTE MACHINE]`. RAPPEL : aucun commit git sans validation humaine explicite (AGENTS.md) — l'execution du geste reste conditionnee au GO humain.","question":"Q3 — Le lot T306 phase 1 doit-il etre committe MAINTENANT ou GROUPE avec autre chose ?","tranche":"COMMIT IMMEDIAT du lot T306 phase 1, selectif et ISOLE (sans le bruit non lie), avec marqueur [NON TESTE MACHINE]. Ne pas grouper avec C4."},{"action":"Ordre pour CLOS : (1) rejouer gates palier C + G200 + bundle a l'etat final (confirmer zero rouge nouveau), (2) completer la preuve explicite AC3 (verification que le stimulus n'est lu que dans le bloc de simulation gated), (3) commit selectif valide humainement, (4) essai machine phase 3 (coupure/inversion phase sur armoire : arret 3 axes, acquittement front, pas de redemarrage auto — action HUM non delegable), (5) passer contrat PENDING→COMPLETED + cloturer T306 dans TASKS.yaml. Devoir d'alerte restant hors AC : FB_SimBench.st:691 M1_M2_M3_BrakeThermalOk_DI:=TRUE (meme classe : defaut thermique non injectable) a traiter dans un lot dedie — non bloquant pour CLOS phase 1 mais a inscrire.","justification":"Le contrat T306_PHASE1 (status PENDING) exige pour CLOS : implémentation + preuves CI rejouees + recette machine. Liste AC : AC1 ✓ (GVL SimPhaseRotationOk:=TRUE, defaut TRUE, comportement nominal identique), AC2 ✓ (test TC-T306-SAF-003 : sortie suit le stimulus), AC3 ◐ (citation PRG_02:138 + bloc gated : prouve par analyse mais PAS de test dedie hors-simulation), AC4 ◐ (note REX affirme G200 PASS + bundle + palier C sans rouge nouveau — REJOUAGE indispensable a l'etat final), AC5 ◐ (preuve ROUGE/VERT documentee §3.2 — a rejouer pour confirmer), AC6 ✓ (zones forbidden vierges, verifie), AC7 ✓ (PRG_02 fichier=POU, suffixe .st), AC8 ✓ (numstat 21 0 additif pur).","question":"Q4 — Closure du lot T306 phase 1 : quoi manque pour CLOS au sens AC1..AC8 ?","tranche":"Lot NON CLOS a ce jour. Statut AC : 5 ✓ verifies, 1 ◐ (AC3 preuve implicite), 1 conditionnel (AC4 rejeu requis), 1 a confirmer au rejouage (AC5). Manquent : rejeu des preuves, commit valide, essai machine, passage contrat PENDING→COMPLETED."}],"constats":[{"point":"VERIFIE — Diff du perimetre autorise T306 propre et coherent : FB_SimBench +3/-1 (entree + passage instSimSafety + ligne 690 TRUE→instSimSafety.SimPhaseRotationOk), FB_Sim_Safety +7 (entree+sortie+propagation, FALSE hors Enable), GVL +4 (SimPhaseRotationOk:=TRUE, nom canonique conforme), PRG_02 +2 (ligne de cablage unique), test +21 additif.","preuve":"git diff + git diff --numstat (5 fichiers : 3 1 / 7 0 / 4 0 / 2 0 / 21 0) — exécuté ce turn"},{"point":"VERIFIE — Zones forbidden (B_AU_SECURITE, H_TREUILS_BENNE, I_TRANSLATION, J_SUPERVISION, DOC/AF, DOC/STDS, PRJ_CODESYS) : VIDE.","preuve":"git status --short sur ces chemins = aucune sortie (AC6 satisfait)"},{"point":"VERIFIE — Zero suppression dans tout le diff et aucun ecrasement T383 : PRG_02 ne porte que +2 lignes T306 (numstat 2 0) ; la modif AX15D/T383 a ete commitee precedemment en 0358c631.","preuve":"git diff --diff-filter=D vide ; git log --oneline -> 0358c631 feat(cycle,...): T383 ..."},{"point":"VERIFIE — Additivite du test de l'entree FB_Sim_Safety : numstat 21 0 (0 suppression), 3 assertions du nouveau TC-T306-SAF-003 ; assertions pre-existantes (TC-P13-SAF-001/002) non touchees (les +21 sont toutes a la fin du fichier).","preuve":"git diff --numstat TOOLS/.../tests/test_fb_sim_safety.st = '21 0' ; contenu du hunk (fin de fichier)"},{"point":"VERIFIE — Les 3 lignes sensibles sont conformes au patron existant et au nom canonique fige : (a) FB_SimBench:690 substitution TRUE→instSimSafety.SimPhaseRotationOk ; (b) PRG_02 cablage unique (motif identique SimChainOkBit :335) ; (c) FB_Sim_Safety : sortie FALSE quand NOT Enable + propagation inconditionnelle du bit quand Enable -> coherent avec le gating SimulationModeActive AND SimSafetyActive.","preuve":"diff hunks : FB_SimBench @@ -687,7 +689,7 @@ ; PRG_02 @@ -333,6 +333,8 @@ ; FB_Sim_Safety @@ -77,6 +79,7 @@ (IF NOT Enable THEN ... := FALSE) + fin de corps"},{"point":"DEDUIT — Le lot est NON COMMITE : working tree = 5 M sur le perimetre + note REX non suivie. De plus, bruit non lie important a ne PAS committer : snapshots troubleshooting (3 CSV), contrats T364/T374 (5), runs orchestrateur (dsh-ha-orchestrator.run-*, runs.jsonl), CODE_XML d'autres lots (D_JOYSTICK/G_CYCLE/J_SUPERVISION/PRG_03/PRG_07), index.html, variable_lists.","preuve":"git status --short : M sur les .st du lot + 3 CSV + 2+3 .md contrats + 3 runs .run-* + runs.jsonl + nb CODE_XML"},{"point":"DEDUIT — Lot pas encore CLOS : AC1 ✓ / AC2 ✓ / AC3 ◐ (preuve implicite par gating, pas de test dedie) / AC4 ◐ (rejeu gates+G200+bundle requis) / AC5 ◐ (ROUGE/VERT a rejouer) / AC6 ✓ / AC7 ✓ / AC8 ✓. Contrat status PENDING.","preuve":"contrat TASK_CONTRACT_T306_PHASE1 yaml : status: PENDING (l.99) + bloc acceptance AC1..AC8 ; note REX §3 (preuves affirmees, non rejouees a ce jour)"},{"point":"DEDUIT — Le cadrage C4 (SafeStop M3 inerte + gate MAINT_N2 bypass) est un correctif de securite hors phase 1, gratuit a cadrer en parallele ; la regle de campagne (SKILL.md§Campagnes) groupe les ESSAIS/import, pas le cadrage.","preuve":"note REX §6 (les 2 correctifs de securite restent hors phase 1, lot C4 separe) ; SKILL orchestrator §296-323 (regroupement obligatoire des essais/imports)"}]}
J'ai lu les 5 documents obligatoires, le diff réel du périmètre autorisé (numstat + hunks), et vérifié les zones forbidden. Je dispose de toutes les preuves pour trancher.

## Synthèse de l'analyse

**Diff du périmètre autorisé T306 phase 1** (VERIFIE, git diff sur le dépôt) :
- `FB_SimBench.st` : **+3/-1** — nouvelle entrée `SimPhaseRotationOkBit`, passage à `instSimSafety`, substitution de la ligne 690 `Machine.PhaseRotationOk_DI := TRUE` → `:= instSimSafety.SimPhaseRotationOk`
- `FB_Sim_Safety.st` : **+7** — entrée `SimPhaseRotationOkBit` + sortie `SimPhaseRotationOk` + propagation (FALSE hors Enable, bit sinon) — cohérent avec sécurité (banc ne publie que sous `SimulationModeActive AND SimSafetyActive`)
- `GVL_Simulation.st` : **+4** — stimulus `SimPhaseRotationOk := TRUE` (défaut TRUE, nom canonique figé conforme)
- `PRG_02_Acquisition.st` : **+2** — ligne unique de câblage `SimPhaseRotationOkBit := GVL_Simulation.SimPhaseRotationOk` (numstat 2 0, pas d'écrasement T383)
- `test_fb_sim_safety.st` : **+21** (numstat `21 0`, additif pure, AC8 ✓)
- **Zones forbidden : VIDE** (AC6 ✓) · **0 suppression** dans tout le diff

**État du dépôt** : lot NON commité. Bruit non lié considérable (snapshots troubleshooting, contrats T364/T374, runs orchestrateur `dsh-ha-orchestrator.run-*`, CODE_XML régénérés d'autres lots) — **un commit propre exige un add sélectif du lot uniquement**.

Je rends le verdict structuré.

## reviewer [via reviewer] [completed]

Restitution vérifiée de première main (état git réel, hash-objects recalculés, lecture des fichiers clés) — pas de récitation des sous-tâches.

---

# 📋 RESTITUTION EXPLOITANT — LOT T306 PHASE 1 — DÉCISIONS

> Aucune conclusion sans preuve. Vérifications rejouées ce tour : `git log/status/hash-object`, lecture `FB_TranslationOutputInterlock.st`, `PRG_04/05/06`, `PRG_02`, `SVN`-dero commit.

---

## 1. TABLEAU D'IDENTITÉ DE RÉVISION

**⚠️ LE LOT N'EST PAS COMMITTÉ.** Le diff vit dans la working tree. HEAD effectif = `9d6f99a9…` (2 commits doc/contrat après `0358c631`, n'affectant pas les 5 fichiers du lot — le diff du lot est identique à celui annoncé).

| Fichier du lot | Parent rev | hash git BLOB (contenu NON COMMITTÉ) |
|---|---|---|
| `CODE/L_SIMULATION/GVL_Simulation.st` | 0358c631 (T383) | `30e376fc0fc7c1b7439d1c1b1a1cf33edea224ef` |
| `CODE/L_SIMULATION/FB_Sim_Safety.st` | 〃 | `b102b0dc27e72b00fe581196fd2be3f2e4a9637f` |
| `CODE/L_SIMULATION/FB_SimBench.st` | 〃 | `8c94038bbb6e772e1309f79f70bafc63be9e5d3a` |
| `CODE/M_MAIN/PRG_02_Acquisition.st` | 〃 | `2573f446de8f458ce7bf3cd99a92f0daa1cb4f1e` |
| `TOOLS/TEST_AUTO_CI/RESULTS/L_SIMULATION/tests/test_fb_sim_safety.st` | 〃 | `c294737b403b84dc1b75745f92700cf699eebef9` |

⚠️ La working tree contient **du bruit non lié au lot** (snapshots troubleshooting, contrats T364/T374, runs orchestrateur, rapports CI d'autres axes, `CODE_XML` d'autres lots) → **commit sélectif obligatoire**, jamais `git add .`.

---

## 2. CHALLENGE DU LOT T306 PHASE 1 — POINT PAR POINT

**Verdict global : NUANCE** — la « plomberie d'injection » est réelle, propre, et **inerte en production**. Mais la preuve par mutation est **quasi-tautologique** et la note REX **sur-déclare** (« injectable et vérifié » de bout en bout alors que seule la feuille FB est exécutée).

**(a) Preuve que le stimulus est INERTE en production — CONFIRMÉ**

Double garde indépendante, rejouée :
- `PRG_02_Acquisition.st:289` `Enable := GVL_Simulation.SimulationModeActive` → le bloc simulé de `FB_SimBench` n'est exécuté que si **mode sim actif**.
- `PRG_02:138` `MachineInputSourceSimulated := SimulationModeActive AND SimSafetyActive` — arbitrage subordonné au mode sim.
- `PRG_02:448` `HwIn.Machine := SEL(MachineInputSourceSimulated, HwReal.Machine, HwSim.Machine)` → en production sélectionne **HwReal**.
- `PRG_02:190` `HwReal.Machine.PhaseRotationOk_DI := PhaseRotationOk_DI` (source réelle) ; `PRG_02:337` `SimPhaseRotationOkBit := GVL_Simulation.SimPhaseRotationOk`.
- `FB_SimBench.st:258` `IF NOT Enable THEN ... Machine := HwReal.Machine; RETURN` (+ :692 câblé sous cette garde), et l'image machine n'est publiée que sous `SimulationModeActive AND SimSafetyActive`.

→ En production (SimulationModeActive=FALSE), aucun chemin n'amène le stimulus dans `HwIn.Machine.PhaseRotationOk_DI`. Scénarios « Enable forcé »/« autre écriture » : **REFUTÉS**, l'aiguillage `SEL :448` ne bascule jamais sans `SimulationModeActive AND SimSafetyActive`.

**(b) Validité réelle de la preuve par mutation — CONFIRMÉE (la preuve est faible)**

`test_fb_sim_safety.st:43-62` (TC-T306-SAF-003) instancie `FB_Sim_Safety` seul, force `Enable:=TRUE`, et `ASSERT fb.SimPhaseRotationOk`. Le « ROUGE sur mutation » échoue parce que le test lit **exactement la sortie du code muté** → dépendance directe/circulaire, **pas une mutation forte**. Ce test passerait même si le câblage GVL→PRG_02→FB_SimBench→HwIn était cassé. **AC2 (« forcer le stimulus à FALSE ⇒ PhaseRotationOk_DI=FALSE ») n'est PAS prouvé par exécution — seulement déduit.** Le commentaire l.54 « PhaseRotationOk_DI tombe » est trompeur : l'assertion porte sur `fb.SimPhaseRotationOk`, jamais sur `PhaseRotationOk_DI`.

**(c) Défauts trouvés — impact sur l'acceptation de l'orchestrateur**

| # | Constat | fichier:ligne | Gravité | Verdict challenge |
|---|---|---|---|---|
| Q1 | Inertie réelle en prod, double garde | PRG_02:138/:289/:448/:190 ; FB_SimBench:258 | INFO | **CONFIRME** (le lot ne peut pas atteindre la prod) |
| Q2 | Preuve par mutation quasi-tautologique, AC2 non prouvée par exécution | test_fb_sim_safety.st:43-62 | MAJOR | **CONFIRME** |
| Q2bis | Note REX « injectable et vérifié » sur-déclare (seule exécution = test feuille) | NOTE_APPLICATION_T306…:27-44 | MINOR | **NUANCE** |
| Q3 | Point d'injection via `FB_Sim_Safety` = bonne couche, polarité TRUE=sain cohérente | FB_Sim_Safety:156-162 ; GVL:34 ; FB_Safety_Translation:22 | INFO | **CONFIRME** |
| Q3bis | Biais polarité branche NOT-Enable : `:82 SimPhaseRotationOk:=FALSE` ; pas de remise à défaut en sortie sim ; stale à la ré-entrée | FB_Sim_Safety:79-82 | MINOR | **NUANCE** (dormant, doc-only) |
| Q4 | Aucune régression/effet de bord : producteur unique, SimSafetyActive intact, 0 lecture GVL ajoutée | FB_SimBench:64/:678-692 ; PRG_02:89-121 | INFO | **REFUTE** (pas d'impact) |
| Q5 | **Trous de preuve AC2/AC5 end-to-end : l'effet sécurité (arrêt axes M1/M2/M3) reste NON PROUVÉ** | contrat:14-32 | MAJOR | **CONFIRME** |

**Impact sur l'acceptation** : l'acceptation est **VALIDÉE sur le périmètre strict « plomberie d'injection feuille »**, mais **INVALIDÉE sur toute revendication « bout en bout vérifié »**. La note REX doit être **corrigée** (ne pas dire « vérifié » end-to-end), et l'acceptation conditionnée à un **test d'intégration** réel.

---

## 3. LES 3 QUESTIONS — TRANCHÉES (pas reposées)

**Q1 — Présenter le diff hunk par hunk ? → NON.**
Le diff du périmètre autorisé est petit (+3/-1 FB_SimBench, +7 FB_Sim_Safety, +4 GVL, +2 PRG_02, +21 test additif pur, 0 suppression) et la relecture indépendante se fait sur le diff réel lu par l'orchestrateur (règle R2), pas sur une re-présentation de l'agent producteur. Un round-trip ajoute zéro vérification mécanique.

**Q2 — Préparer le lot C4 maintenant ? → OUI, en parallèle, immédiatement.**
Le cadrage (contrat + brief) est un travail documentaire d'orchestrateur sans coût de session humaine → hors règle de campagne. La gravité le justifie (2 écarts sécurité confirmés en phase 0). Ça ne bloque pas le commit de T306 ; seule contrainte = collision d'écriture `PRG_02` (un seul écrivain par fichier à vérifier AVANT l'implémentation C4).

**Q3 — Commit du lot T306 maintenant ou groupé ? → COMMIT IMMÉDIAT, sélectif et ISOLÉ, avec marqueur [NON TESTE MACHINE], subordonné au GO humain explicite.**
Ne pas grouper avec C4. Lot fermé sur l'implémentation (gates palier C + G200 PASS, zones forbidden vierges, 0 suppression). Précédent T383 = committé en `0358c631` avec `[NON TESTE MACHINE]` avant recette → le commit précède l'essai machine, conforme au pattern 2 temps d'AGENTS.md. **Rappel absolu** : aucun commit sans validation humaine explicite — le geste reste conditionné au GO humain.

---

## 4. EXPERTISE SÉCURITÉ — LES 2 CORRECTIFS

**ÉCART A — SafeStop M3 inerte à la barrière finale (`FB_TranslationOutputInterlock.st:15` déclaré, JAMAI lu dans le corps :69-156, alors que câblé en entrée `PRG_06_Outputs.st:438`)**
- **Gravité réelle NUANCÉE** : M3 **s'arrête quand même** via la rampe de `FB_Translation` (`PRG_05:575 instTranslationM3.SafeStop := M3_SafeStop_Aggregate` ; agrégat `:511-512`) et le permis effectif est retiré (`:541-547`). Le défaut n'est donc **pas « aucun arrêt »**, c'est une **perte de défense en profondeur** : la barrière finale (frein/couple) ne force pas `BrakeCmd:=FALSE` si `FB_Translation` défaillait coïncidemment. Sur un axe porteur de charge → **personnes exposées**. **Gravité = MAJOR**, à traiter en **C4** avec double revue.

**ÉCART B — Bypass PhaseRotation non gaté MAINT_N2 sur 3 axes (`PRG_04:984` M1, `:1050` M2 câblé brut GVL_IHM — commentaire `:970` « SANS gate » ; `PRG_05:496` M3 non gaté alors que le voisin `BypassLimitSwitch` est gaté `M3_MaintN2` `:500`/`:603`, défini `:462`)**
- **Gravité réelle la PLUS ÉLEVÉE** : un défaut rotation de phases (inversion/perte ⇒ sens/rotor erroné ⇒ charge non maîtrisée) est **totalement masqué en PRODUCTION** (SEMI_AUTO/AUTO) → aucun arrêt exigé. **BLOCK**, C4.
- **Complément B — obligatoire** : gater le seul `BypassPhaseRotation` est **INCOMPLET et créé une fausse assurance** : la cause 4 est neutralisée par `NOT (BypassProcess OR BypassPhaseRotation)` → `BypassProcess` seul lève aussi la protection, y compris via le bypass commun SlackCable (`FB_Safety_Winch.st:302`, `FB_Safety_Translation.st:140`). **Désolidariser la cause rot.phase de BypassProcess.**

**Dérogation MES 2026 — RÉELLE mais ne justifie PAS la non-protection en prod**
- Vérifié : commit `cac7387d` (Mathieu Gros, 2026-09-02, `feat(safety): DEROGATION MES … sans gate de mode [NON TESTE]`), documenté `AF_Partie-05…:179-192`, marqué **« écart assumé… NON conforme ISO 13849… à revoir… une fois la MES stabilisée »**. **TEMPORAIRE, [NON TESTE], à périmètre dérogé (bypass comm/position non-critiques).** `PhaseRotation` protège des **charges lourdes + personnes** → il ne fait PAS partie des bypass dérogeables. À laisser gaté MAINT_N2.

**Ordre de priorité** : 1. ÉCART B (masquage en production = plus grave) → 2. ÉCART A (défense en profondeur), **dans le MÊME lot de sécurité**.

**RECOMMANDATION D'ACTION UNIQUE ET EXÉCUTABLE** :
> **Un agent (DSH01) prépare et livre UN lot unique « T306 phase 2 — correctifs sécurité » en C4** (contrat `TASK_CONTRACT` + double revue exigés) qui :
> **(a)** dans `CODE/I_TRANSLATION/FB_TranslationOutputInterlock.st` au bloc de décision finale (§3/:137, :140-146) force `BrakeCmd := FALSE` + bloque le mouvement (DriveControlWord:=0) quand `SafeStop=TRUE`, **sans toucher** Reset/Enable/FaultCore/watchdog/PDO ;
> **(b)** gate sur MAINT_N2 `BypassPhaseRotation` sur `PRG_04_Treuils_Benne.st:984/:1050` (via `PRG_03_Modes_Cycle.Data.Auth.Mode = E_Mode.MAINT_N2`) et sur `PRG_05_Translation.st:496` via `M3_MaintN2` (:462) ;
> **(c)** désolidarise la cause rot.phase de `BypassProcess` dans `FB_Safety_Winch.st:302` et `FB_Safety_Translation.st:140` ;
> puis génère bundle complet + diff bundle, exécute `G200_check_linkage.py --report` (PASS 0 erreur) + `run_all_gates.py` (**G483 repassé au vert**), et prouve ROUGE/VERT un test d'injection défaut rot.phase VERT sur M1/M2/M3 avec redondance finale vérifiée sur M3 et bypass **inopérant hors MAINT_N2**. Livraison à la **même campagne d'essais machine** que T306 phase 1.

---

## 5. POINTS INDÉTERMINÉS & CE QU'IL FAUT MESURER

| # | Indéterminé | Mesure requise |
|---|---|---|
| I1 | **Injection end-to-end non prouvée par exécution** (seule feuille testée) | Ajouter un test d'intégration `PRG_02`/`test_main_end_to_end` : forcer `GVL_Simulation.SimPhaseRotationOk=FALSE` et **observer** `HwIn.Machine.PhaseRotationOk_DI=FALSE` (matérialise AC2), + test réaction 3 axes (SafeStop/latch/interlocks/freins) + test latch/Reset front sans réarmement auto |
| I2 | AC3 (stimulus lu **seulement** dans le bloc sim gated) | Test dédié hors-simulation (actuellement preuve déductive par `PRG_02:138/:289`, pas exécutée) |
| I3 | Preuves CI à l'état final (G200, gates palier C, bundle) | **REJOUAGE** complet avant CLOS du contrat |
| I4 | Périmètre exact de la désolidarisation rot.phase vs `BypassProcess` (routage mou-de-câble/`SlackCable` conservé `PRG_04:508/:520/:974/:1042`) | Confirmer par arbitrage avant implémentation C4 |
| I5 | Signalisation IHM du bypass (0 fichier `.vis`) | Complément non bloquant (si option « laisser + signaler » — non retenue) |
| I6 | Stabilité MES : besoin N1 dérogé = bypass non-critiques (comm/drive) uniquement ? | Confirmation exploitant avant de figer le périmètre dérogé restant |
| I7 | `FB_SimBench.st:691 M1_M2_M3_BrakeThermalOk_DI:=TRUE` (même classe : défaut thermique non injectable) | Lot dédié, **non bloquant** pour CLOS phase 1 mais à inscrire au registre |
| I8 | Test `FB_Sim_Safety` assertion sur `fb.SimPhaseRotationOk` vs `PhaseRotationOk_DI` (l.54 trompeuse) | Corriger la note + compléter l'assertion de bout en bout |

---

**Récapitulatif décisions** : 🔒 Commit T306 **immédiat, sélectif, isolé** `[NON TESTE MACHINE]` après GO humain · 📋 Cadrage C4 **maintenant en parallèle** · 🛡️ Lot sécurité **T306 phase 2 unique** (Écart B puis A) en C4, livré à la **même campagne d'essais** · ✍️ Corriger la note REX (retirer « vérifié » end-to-end) · ⏸️ CLOS contrat subordonné au **rejeu CI + commit validé + essai machine**.

## supervisor#1 [completed]

La restitution est terminée et complète ci-dessus. Aucune nouvelle question ou demande n'apparaît dans ce tour, donc je ne relance pas de travail redondant.

**Récapitulatif exécutif** (pour l'exploitant qui n'a pas le temps) :

| Décision | Verdict |
|---|---|
| 🔒 Commit lot T306 phase 1 | **IMMÉDIAT, sélectif, isolé** `[NON TESTE MACHINE]` — subordonné au **GO humain explicite** (règle AGENTS.md) |
| 📋 Préparer lot C4 maintenant ? | **OUI, en parallèle** (cadrage documentaire gratuit ; collision `PRG_02` à vérifier avant implémentation) |
| 🛡️ Lot sécurité T306 phase 2 | **UN lot unique C4** : ÉCART B (gate MAINT_N2 + désolidarisation BypassProcess) **puis** ÉCART A (SafeStop M3 → `BrakeCmd:=FALSE`) — livré à la **même campagne d'essais** que T306 |
| ✍️ Note REX | **Corriger** : retirer « vérifié » end-to-end (seule la feuille FB est exécutée) |
| ⏸️ CLOS contrat | Subordonné au **rejeu CI + commit validé + essai machine** (action HUM non déléguable) |

**Point critique sécurité** : la dérogation MES 2026 (`cac7387d`) est **réelle mais [NON TESTE] et non conforme ISO 13849** — elle ne justifie **pas** de laisser `BypassPhaseRotation` neutralisable en production (masque défaut rotation de phases sur 3 axes porteurs de charge).

L'action unique et exécutable reste : **un agent (DSH01) prépare et livre le lot « T306 phase 2 — correctifs sécurité » en C4**, exactement comme spécifié au §4 de la restitution, avant toute mise en production.

Dites-moi que vous validez (ou non) et je peux soit (a) lancer le cadrage C4 en parallèle, soit (b) préparer le commit sélectif T306 sur validation humaine. Je ne committe rien sans votre accord explicite.

## supervisor#2 [completed]

La restitution est terminée et complète — elle a été livrée au tour précédent. Aucune nouvelle demande n'apparaît dans ce tour, je ne relance donc aucun travail redondant et je ne committe rien.

**État actuel — 2 décisions en attente de l'exploitant (action HUM non déléguable) :**

| Décision | Statut |
|---|---|
| 🔒 Commit sélectif lot T306 phase 1 `[NON TESTE MACHINE]` | **EN ATTENTE de GO humain explicite** (règle absolue AGENTS.md) |
| 📋 Cadrage lot C4 (contrat + brief) | **Disponible au lancement immédiat** en parallèle, sans bloquer le commit |

**Ce qui est déjà acté (n'a pas besoin de nouvelle validation) :**
- Verdict challenge T306 : **NUANCE** — stimulus inerte en prod (double garde), mais preuve par mutation quasi-tautologique et note REX à corriger (retirer « vérifié » end-to-end).
- Lot sécurité : **1 lot unique « T306 phase 2 »** (Écart B puis Écart A) en C4, périmètre fixé aux fichiers/lignes cités (§4 de la restitution), livré à la **même campagne d'essais** que T306.
- CLOS contrat T306 subordonné au **rejeu CI + commit validé + essai machine**.

Je reste en attente de votre décision : **(a)** lancer le cadrage C4, et/ou **(b)** préparer le commit sélectif T306 pour soumission à validation humaine. Je n'exécute aucun des deux tant que vous n'avez pas tranché.

## summary

supervisor 结论：La restitution est terminée et complète — elle a été livrée au tour précédent. Aucune nouvelle demande n'apparaît dans ce tour, je ne relance donc aucun travail redondant et je ne committe rien.

**État actuel — 2 décisions en attente de l'exploitant (action HUM non déléguable) :**

| Décision | Statut |
|---|---|
| 🔒 Commit sélectif lot T306 phase 1 `[NON TESTE MACHINE]` | **EN ATTENTE de GO humain explicite** (règle absolue AGENTS.md) |
| 📋 Cadrage lot C4 (contrat + brief) | **Disponible au lancement immédiat** en parallèle, sans bloquer le commit |

**Ce qui est déjà acté (n'a pas besoin de nouvelle validation) :**
- Verdict challenge T306 : **NUANCE** — stimulus inerte en prod (double garde), mais preuve par mutation quasi-tautologique et note REX à corriger (retirer « vérifié » end-to-end).
- Lot sécurité : **1 lot unique « T306 phase 2 »** (Écart B puis Écart A) en C4, périmètre fixé aux fichiers/lignes cités (§4 de la restitution), livré à la **même campagne d'essais** que T306.
- CLOS contrat T306 subordonné au **rejeu CI + commit validé + essai machine**.

Je reste en attente de votre décision : **(a)** lancer le cadrage C4, et/ou **(b)** préparer le commit sélectif T306 pour soumission à validation humaine. Je n'exécute aucun des deux tant que vous n'avez pas tranché.
