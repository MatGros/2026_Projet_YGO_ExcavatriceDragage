# Contrat T174 — Tests CI du socle FaultCore + Safety EmergencyManagement
# Source des écarts : DOC/WFLOW/P3_TEST_AUDIT_PROPOSITIONS_v1.0.md (audits P3-1a..d, 2026-08-29)

contract:
  task_id: T174
  criticality: C3            # preuve CI manquante sur le socle transverse Fault (18 FB standard) + fiche Safety V-I à prouver
  strategy: patch

  objective: >
    Rendre prouvées par CI les fonctions socle FB_FaultCore (TC-P03-008..013 — SOCLE P0 :
    test_fb_fbstatus.st vise un FB supprimé au commit 51fccce6, ne compile plus) et
    compléter la couverture FB_Safety_EmergencyManagement : 10/12 TC déjà prouvés par
    rapport daté (2026-08-29 12:50:02, 10/10 PASS) — restent TC-P01-001 (essai SITE,
    non automatisable par nature) et TC-P01-005 (af_ignore).

  acceptance:
    - id: AC0
      statement: "Preuve existante : test_fb_safety_emergency.st (10/12 TC) et son rapport reports/FB_Safety_EmergencyManagement.json (10/10 PASS, 2026-08-29 12:50) restent verts après le lot (aucune régression du fichier existant)."
      verified_by: "re-run harnais TEST_AUTO_CI sur la fiche : 10/10 PASS conservés"
    - id: AC1
      statement: "Un fichier test_fb_faultcore.st couvre TC-P03-008..013 (latch cause, disparition cause + Reset sur front, priorité causes, masque, indépendance d'instances) et compile contre FB_FaultCore.st actuel."
      verified_by: "pytest TOOLS/AGENT_WORKFLOW/tests/test_g450_af_ci_coverage.py + run du harnais TEST_AUTO_CI (G460) : 0 échec"
    - id: AC2
      statement: "TC-P01-005 : soit l'af_ignore est justifié par une note datée (test logique minimal 'ArmRequest sans Reset bloqué, puis Reset+ArmRequest arme' ajouté si retenu), soit un TEST correspondant est ajouté au fichier existant."
      verified_by: "lecture registry.yaml + run harnais"
    - id: AC3
      statement: "Chaque TEST porte un titre contenant l'identifiant TC couvert et chaque TC est enregistré dans TOOLS/TEST_AUTO_CI/registry.yaml (zéro TC-P03-008..013 / TC-P01-* absent du registry)."
      verified_by: "G450_check_af_ci_coverage.py --report : disparition des WARN AF-03/AF-01 correspondants"
    - id: AC4
      statement: "L'entrée af_ignore: [TC-P01-005] de registry.yaml est soit justifiée par un commentaire factuel (action 2 temps = acquittement IHM puis réarmement pupitre = test site), soit supprimée si un TEST automatisable la couvre."
      verified_by: "lecture registry.yaml + note de justification datée"
    - id: AC5
      statement: "Aucun etat de TC (V, V-I, NV, NV-I, R, NA) n'est modifie dans DOC/AF : le lot ne touche QUE l'outillage CI (les etats evolueront ensuite sur preuves d'execution)."
      verified_by: "git diff -- DOC/AF vide en fin de lot"

  scope:
    allowed:
      - TOOLS/TEST_AUTO_CI/TESTS/**
      - TOOLS/TEST_AUTO_CI/registry.yaml
      - TOOLS/TEST_AUTO_CI/RESULTS/** (rapports de run)
    forbidden:
      - CODE/**                    # aucun changement de comportement machine dans ce lot
      - DOC/AF/**                  # aucun changement d'état de TC ici
      - PRJ_CODESYS/PROJ_Full_ImportExport/Device.export
      - DOC/WFLOW/TASKS.yaml       # la tâche est écrite une fois ici, pas par le lot

  evidence_required:
    - run_all_gates          # G420/G460 (pytest + harnais)
    - G450_report            # couverture AF -> TC -> TEST_AUTO_CI
    - pytest_gates           # test_extract_functions_matrix / test_generate_af_viewer

  alert_duty: >
    Si FB_Safety_EmergencyManagementLogic.st ne permet pas de forcer proprement les entrées
    des TC (pas de points d'injection), lever l'alerte IMMÉDIATEMENT (le lot devient
    "contrat d'instrumentation" à arbitrer) — jamais approximer un scénario.

status: PENDING

execution:
  executed_by: ""
  completed_at: ""
  status: PENDING
  summary: >
    À remplir par l'agent exécutant : fichiers de test créés, résultats harnais, registry.
  evidence_submitted: []

validation:
  status: PENDING
  validated_by: ""
  validated_at: ""
  notes: >
    Rappel : pré-condition humaine — arbitrage de l'insertion des 63 TC nouveaux (annexe
    P3_TEST_AUDIT_PROPOSITIONS_v1.0.md) attendu pour les TC suffixés ; ce lot T174 vise
    d'abord les TC RACINES existants déjà décrits dans les fiches.