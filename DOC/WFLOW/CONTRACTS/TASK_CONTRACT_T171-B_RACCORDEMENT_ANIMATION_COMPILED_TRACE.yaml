contract:
  task_id: T171-B_RACCORDEMENT_ANIMATION_COMPILED_TRACE
  parent_id: T171
  criticality: C2
  strategy: new_feature

  depends_on:
    - T171-A

  objective: >
    Remplacer intégralement le moteur JS fictif dans FICHE_SEMI_AUTO_ANIMATION.html
    (document interactif authored) par un lecteur Canvas dynamique alimenté exclusivement par
    la trace réelle scan-par-scan produite par le binaire compilé de FB_Cycle (trace_semi_auto_cycle.json).
    L'ancienne simulation JS pure est définitivement supprimée pour bannir toute double vérité.

  acceptance:
    - id: AC1
      statement: "100% de la cinématique Canvas (moteurs treuils, chariot M3, mâchoires benne, capteurs eau/fond) est asservie aux sorties du binaire compilé (WinchM1/M2Cmd, TranslationCmd, BucketCmd)."
      verified_by: "Vérification de l'asservissement frame Canvas = scan N du JSON."
    - id: AC2
      statement: "Le document HTML expose explicitement window.__traceScan et permet un contrôle interactif (Play, Pause, Vitesse x1/x2/x5, Scrubbing direct au scan voulu) avec affichage temps-réel des E/S."
      verified_by: "Test fonctionnel dans le navigateur et contrôle d'inspection DOM."
    - id: AC3
      statement: "L'orchestrateur lit, vérifie et valide le git diff réel (visa P5)."
      verified_by: "Visa formel de l'orchestrateur."

  scope:
    allowed:
      - TOOLS/TEST_AUTO_CI/RESULTS/G_CYCLE/reports/FICHE_SEMI_AUTO_ANIMATION.html
    forbidden:
      - CODE/**
      - PRJ_CODESYS/**

  conservation:
    must_survive:
      - "Qualité du rendu visuel Canvas, ergonomie de la télémétrie et du bandeau opérateur."
    dropped_on_purpose:
      - "Machine d'état JS simulée interne (switch STATE.step codé en dur)."

  evidence_required:
    - html_report_pass
    - zero_fictitious_logic_proof

status: READY_FOR_EXECUTION
