# ═══════════════════════════════════════════════════════════════════════════
# CONTRAT DE TACHE ET SUIVI DE CYCLE DE VIE — projet Excavatrice Dragage
# ═══════════════════════════════════════════════════════════════════════════

contract:
  task_id: T167-C
  criticality: C4
  strategy: patch

  objective: >
    Durcir FB_ExtractionSequence en ajout additif sur la machine d'état existante (T167-A §3bis) :
    backstop de fermeture benne (CfgBucketCloseTimeout) verrouillé au runtime pour rester
    superieur au watchdog mouvement de FB_Bucket (BucketMoveTimeout) — ne jamais masquer BucketError ;
    timeout de garde CONTROL_ASCENT calcule au runtime depuis ControlAscentDistance_M ;
    capture StepAtFault avant bascule ERROR_HOLD ; accumulateurs anti-pompage homme-mort ;
    latch survivant au cycle Enable, Reset jamais conditionne.

  acceptance:
    - id: AC1
      statement: "FB_ExtractionSequence gère la séquence : Attente fond -> Fermeture benne -> Décollage contrôlé -> Fin d'assistance."
      verified_by: "Revue ST et validation G200."
    - id: AC2
      statement: "L'effacement de BottomPositionConfirmed est verrouillé et confirmé consommé (BottomConfirmationConsumed)."
      verified_by: "Revue ST et tests unitaires."
    - id: AC3
      statement: "Neutralisation déterministe sur NOT Enable et capture ExtractionStateAtError sur tout blocage sans réarmement automatique."
      verified_by: "Gate G127 PASS et revue logique."
    - id: AC4
      statement: "L'orchestrateur lit, vérifie et valide le git diff réel ligne par ligne."
      verified_by: "Visa formel de l'orchestrateur"

  scope:
    allowed:
      - CODE/G_CYCLE/FB_ExtractionSequence.st
      - CODE/J_SUPERVISION/_TYPES/3_CYCLE_ET_MODES/E_ExtractionSequenceState.st
      - CODE/J_SUPERVISION/_TYPES/3_CYCLE_ET_MODES/ST_SequencePublicState.st
      - CODE/J_SUPERVISION/_TYPES/5_ASSISTANCE_DRAGAGE/ST_DredgingAssistState.st
      - CODE/M_MAIN/PRG_03_Modes_Cycle.st          # cablage CfgBucketCloseTimeout / BucketMoveTimeout / ExtractionStepAtFault (visa orchestrateur)
      - CODE/M_MAIN/PRG_07_Supervision.st          # projection ExtractionStepAtFault vers GVL_IHM
      - DOC/AF/AF_Partie-04_Mode_SemiAuto_Sequenceur_v2.3.md  # T167-A §3bis points 3 et 4
      - TOOLS/TEST_AUTO_CI/RESULTS/G_CYCLE/tests/test_fb_extractionsequence.st
      - CODE_XML/CODE_Bundle.xml
      - DOC/WFLOW/CONTRACTS/TASK_CONTRACT_T167-C_REFONTE_EXTRACTION.yaml
    forbidden:
      - CODE/M_MAIN/PRG_04_Treuils_Benne.st
      - CODE/G_CYCLE/FB_DiveSearch.st
      - PRJ_CODESYS/PROJ_Full_ImportExport/Device.export

  conservation:
    must_survive:
      - "Interface consommée par PRG_03.Data (BucketCloseRequest, ForceMinSpeedStep)."
      - "Plafonnement au Palier 1 pendant l'extraction."
    dropped_on_purpose:
      - "None"

  evidence_required:
    - check_linkage
    - run_all_gates
    - bundle

  models_allowed:
    - "omni/cc/claude-sonnet-5"
    - "omni/cx/gpt-5.6-terra"
    - "antigravity"

  alert_duty: >
    Tout comportement non linéaire sur l'extraction ou le forçage de vitesse doit être
    remonté immédiatement.

status: VALIDATED   # visa humain 2026-08-28 (CODESYS OK) ; 3 revues indépendantes + 2 passes durcissement

execution:
  executed_by: "CC (orchestrateur, mandat autonomie T167)"
  completed_at: "2026-08-28T22:30:00+02:00"
  status: DONE
  summary: >
    Backstop CfgBucketCloseTimeout (defaut T#75s) verrouille au runtime : ErrorCausePresent si
    <= BucketMoveTimeout (ref FB_Bucket.CfgTimeoutDuration cable depuis PRG_03) ou <= T#0ms, idem CycleTime.
    Timeout CONTROL_ASCENT runtime = MAX(0.1, ControlAscentDistance_M)/0.15*2.0 (constantes CST_*).
    Accumulateurs anti-pompage (avancent seulement sous montee demandee). StepAtFault := PrevState avant
    ERROR_HOLD, Reset du StepAtFault conditionne aux memes gardes que le reset du latch.
    AF_Partie-04 §3bis points 3-4 corriges (watchdog benne reel = T#60s, pas T#6s ; backstop, pas seuil primaire).
  evidence_submitted:
    - "FB_ExtractionSequence 8/8 PASS (TC-P04-022 timeout, -023 backstop, -024 latch Enable, -025 coupure meme-scan, -026 F1/F2)"
    - "Non-regression : FB_DiveSearch 8/8, FB_Cycle 7/7, PRG_03 5/5, PRG_07 3/3"
    - "G200 PASS (0 erreur), run_all_gates PASS, G340 doc-links PASS, bundle frais"
    - "T167-CR : 3 revues independantes (deepseek-v4-flash, gpt-5.6-terra-high, gpt-5.6-sol-max)"
    - "2 passes de durcissement : H1-H5 puis F1-F3 (voir DOC/WFLOW/T167-CR_REVUE_INDEPENDANTE_CYCLES.md)"

validation:
  status: VALIDATED
  validated_by: "HUM (visa 2026-08-28, compilation CODESYS 3.5 OK)"
  validated_at: "2026-08-28"
  notes: "3 revues independantes + 2 passes durcissement. Items D1/D2 (recovery reset, garde position basse) = hors scope, planifies §4."
