# ═══════════════════════════════════════════════════════════════════════════
# CONTRAT DE TACHE — T153-C : Application pilote Table des Fonctions AF-08
# + outillage d'extraction — Projet Excavatrice Dragage — CODESYS 3.5
# ═══════════════════════════════════════════════════════════════════════════

contract:
  task_id: T153-C
  parent_id: T153
  criticality: C1            # documentaire + outillage Python, 0 risque fonctionnel CODE/
  strategy: patch

  # ── 1. Objectif ───────────────────────────────────────────────────────────
  objective: >
    Déployer la section "Table des fonctions" (convention finalisée par T153-B)
    sur AF-08 Joystick (resynchronisée par T153-A) et livrer dans le même lot
    le script d'extraction — condition 4 du challenge indépendant : ne pas
    différer l'outillage de validation après le pilote documentaire, sous
    peine de dérive documentaire (REX 2026-07-29, AGENTS.md).

  # ── 2. Critères d'acceptation ─────────────────────────────────────────────
  acceptance:
    - id: AC1
      statement: "AF_Partie-08_Fonction_Joystick (dernière version active) porte une section '🎯 Table des fonctions' au format finalisé par T153-B (colonne Réalisée par étendue à FB/PRG/gate)."
      verified_by: "Lecture du document, présence de la section et de l'ID F08.xx pour chaque fonction identifiée"
    - id: AC2
      statement: "TOOLS/AGENT_WORKFLOW/scripts/extract_functions_matrix.py existe, parcourt DOC/AF/AF_Partie-NN_*.md et produit un YAML/CSV consolidé des tables Fonctions + Points de validation."
      verified_by: "Exécution du script sur AF-08, sortie YAML/CSV valide et non vide"
    - id: AC3
      statement: "Un test unitaire du script existe sous TOOLS/AGENT_WORKFLOW/tests/ et passe."
      verified_by: "python -m pytest TOOLS/AGENT_WORKFLOW/tests/ -q -k extract_functions_matrix = PASS"
    - id: AC4
      statement: "Aucune régression documentaire : gates Palier C 100% PASS (hors échec pré-existant déjà connu, à citer explicitement si rencontré)."
      verified_by: "python TOOLS/AGENT_WORKFLOW/scripts/run_all_gates.py --palier C"

  # ── 3. Périmètre ──────────────────────────────────────────────────────────
  scope:
    allowed:
      - DOC/AF/AF_Partie-08_Fonction_Joystick_v2.1.md (ou version ultérieure issue de T153-A)
      - TOOLS/AGENT_WORKFLOW/scripts/extract_functions_matrix.py
      - TOOLS/AGENT_WORKFLOW/tests/test_extract_functions_matrix.py
    forbidden:
      - CODE/D_JOYSTICK/FB_Joystick.st et toute autre CODE/*.st
      - Toute autre AF que AF-08 (le pilote reste limité à un seul domaine)

  # ── 4. Preuves attendues en restitution ───────────────────────────────────
  evidence_required:
    - run_all_gates
    - pytest_report

  # ── 5. Modèles autorisés ───────────────────────────────────────────────────
  models_allowed:
    - "omni/cc/claude-sonnet-5"
    - "omni/cx/gpt-5.6-terra"

  # ── 6. Devoir d'alerte ────────────────────────────────────────────────────
  alert_duty: >
    Si la convention finalisée par T153-B s'avère impraticable une fois
    appliquée réellement sur AF-08 (ex. cas non couvert), remonter
    IMMÉDIATEMENT à l'orchestrateur plutôt que d'improviser une variante.

status: PENDING

execution:
  executed_by: "..."
  completed_at: "..."
  status: PENDING
  summary: >
    Résumé succinct du travail réalisé par l'agent et résultats des vérifications.
  evidence_submitted: []

validation:
  status: PENDING
  validated_by: "..."
  validated_at: "..."
  notes: >
    Remarques de validation de l'orchestrateur et/ou recettage humain.
