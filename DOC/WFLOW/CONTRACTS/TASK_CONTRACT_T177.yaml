# Contrat T177 — SpeedGuardEnable activé par défaut (D5) + garde vitesse en défaut

contract:
  task_id: T177
  criticality: C3            # activation d'un garde-fou vitesse existant (code déjà présent mais OFF)
  strategy: patch

  objective: >
    DÉCISION T177 TRANCHÉE (2026-08-31) : option (a) — ACTER LE RISQUE.
    SpeedGuardEnable RESTE FALSE par défaut (PRG_04_Treuils_Benne.st:921/969) tant que la
    table SpeedBandMaxMps n'est pas validée en charge réelle (AF-10 §7.3 TBD).
    Cohérent avec T181-01 / T193-S5. Le garde-fou vitesse mesurée reste HORS SERVICE
    (dette safety documentée). La surveillance survitesse réelle est portée par
    FB_Safety_Winch (T181-16, gelée). Ne PAS armer une survitesse sur une table non prouvée.

  acceptance:
    - id: AC1
      statement: "SpeedGuardEnable reste FALSE par défaut au point d'appel (PRG_04:921/969) — la logique de garde-fou FB_Winch §5 (l.219-233) reste volontairement désactivée."
      verified_by: "lecture PRG_04_Treuils_Benne.st:921/969 + FB_Winch.st:223"
    - id: AC2
      statement: "Décision de risque acté documentée dans le contrat T177, avec renvoi explicite à T181-01/T193-S5 et impact safety clair."
      verified_by: "diff contrat T177"
    - id: AC3
      statement: "Aucune modification de code safety : la liaison M1/M2 (SpeedGuardReady, MeasuredSpeedBand) est préservée bit-identique."
      verified_by: "aucun diff CODE/ + G200 liaison"
    - id: AC4
      statement: "Ré-ouverture conditionnelle : T177 redevient exécutable (activation TRUE) uniquement après validation SpeedBandMaxMps en charge réelle (AF-10 §7.3) et livraison T181-01/T181-16."
      verified_by: "suivi T181-01/T181-16"

  scope:
    allowed:
      - CODE/H_TREUILS_BENNE/**
    forbidden:
      - CODE/A_COMMUN/**
      - DOC/AF/**             # la fiche ne bouge qu'après le code, sur preuve
      - PRJ_CODESYS/PROJ_Full_ImportExport/Device.export
      - DOC/WFLOW/TASKS.yaml

  evidence_required:
    - check_linkage          # G200 bloquant sur le bundle (aucun diff CODE/ attendu)
    - run_all_gates          # palier C
    - bundle                 # CODE_Bundle.xml régénéré

  alert_duty: >
    ⚠️ GARDE-FOU SAFETY : ne PAS réactiver la bande vitesse mesurée sur une table non prouvée
    (danger identifié). La conséquence au-delà de SpeedGuardLimited (arrêt puis coupure) n'est
    PAS implémentée — ne pas écrire un TC plus sévère que ce que le code fait. Toute activation
    prématurée de SpeedGuardEnable est un danger machine réel.

status: DECIDED_RISK_ACCEPTED

execution:
  executed_by: "Agent T177 (tranchage décision)"
  completed_at: "2026-08-31"
  status: DECIDED
  summary: >
    Option (a) retenue : ACTER LE RISQUE. SpeedGuardEnable reste FALSE par défaut
    (PRG_04:921/969) — cohérent avec T181-01/T193-S5. La table SpeedBandMaxMps n'est pas
    validée en charge réelle (AF-10 §7.3 TBD) : armer la bande créerait un danger réel
    (clamp palier 1 otage de instWinchSync, cf. commentaire PRG_04:906-910). Aucun diff CODE/.
    Ré-ouverture conditionnelle à T181-01/T181-16 + validation table.
  evidence_submitted: []

validation:
  status: PENDING
  validated_by: ""
  validated_at: ""
  notes: >
    Décision D5 (2026-08-29) : activer par défaut SAUF bypass safety. Ici le bypass safety
    est le risque acté T181-01 : la table n'est pas prouvée. Le garde reste OFF tant que
    AF-10 §7.3 n'est pas livré. Ré-ouvrir T177 à la livraison T181-01/T181-16.