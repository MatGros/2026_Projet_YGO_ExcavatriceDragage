[project]
name = "st2plcopenxml"
version = "0.1.0"
description = "Deterministic ST -> PLCopenXML generator for CODESYS 3.5 SP19"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
