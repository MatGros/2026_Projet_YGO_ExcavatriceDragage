{
  "name": "linter-st",
  "displayName": "Linter ST (CODESYS)",
  "description": "Diagnostics live pour ST CODESYS 3.5 via STruCpp -- panneau Problems a la sauvegarde.",
  "version": "0.6.1",
  "publisher": "excavatrice-dragage",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": ["Linters"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "linterSt.lintSelection",
        "title": "Linter ST : Analyser la sélection"
      },
      {
        "command": "linterSt.lintWorkspace",
        "title": "Linter ST : Analyser tout le code (CODE/)"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "linterSt.lintSelection",
          "group": "7_modification"
        }
      ]
    },
    "configuration": {
      "title": "Linter ST",
      "properties": {
        "linterSt.pythonPath": {
          "type": "string",
          "default": "python",
          "description": "Interpreteur Python utilise pour lancer lint.py."
        },
        "linterSt.codeRoot": {
          "type": "string",
          "default": "CODE",
          "description": "Racine des sources ST, relative au workspace (--code-root passe a lint.py)."
        },
        "linterSt.lintScriptPath": {
          "type": "string",
          "default": "TOOLS/LINTER_ST/lint.py",
          "description": "Chemin vers lint.py, relatif au workspace ouvert (ou absolu). L'extension une fois installee (.vsix) tourne hors du repo -- lint.py doit etre trouve dans le projet ouvert, pas a cote du binaire de l'extension."
        },
        "linterSt.enableSyntaxCheck": {
          "type": "boolean",
          "default": true,
          "description": "Couche base : compilation IEC/CODESYS via STruCpp (erreurs de syntaxe et de typage). Desactiver coupe tous les diagnostics du linter."
        },
        "linterSt.applyCompatFixes": {
          "type": "boolean",
          "default": true,
          "description": "true (defaut) = applique nos correctifs de compatibilite CODESYS (retrait GVL_XXX./PERSISTENT, fusion ARRAY imbrique...) et filtre les faux positifs connus (DEVICE_STATE, GVL/PROGRAM qualifie...) avant d'afficher un diagnostic. false = mode STruCpp BRUT, aucune transformation ni filtrage -- chaque message du compilateur remonte tel quel, meme sur ce qu'on sait etre un faux positif. Utile pour comparer et voir precisement ce que nos correctifs changent (equivaut a lint.py --raw)."
        },
        "linterSt.showIncompleteWarnings": {
          "type": "boolean",
          "default": false,
          "description": "true = affiche un avertissement dans le panneau Problems quand un type externe inconnu empeche la resolution complete. false (defaut) = silence dans Problems (visible uniquement dans la sortie Output > Linter ST)."
        },
        "linterSt.enableProjectRules": {
          "type": "boolean",
          "default": false,
          "description": "Interrupteur general de la couche standards projet (regles maison AGENTS.md -- homme-mort, Reset sur front, etc.). Pas encore implementee (reserve pour un lot futur) -- laisser a false tant qu'aucune regle n'existe."
        },
        "linterSt.projectRules": {
          "type": "object",
          "default": {},
          "description": "Active/desactive chaque regle standard projet individuellement (ex: {\"hommeMort\": true, \"resetOnFront\": true}). Ignore tant que linterSt.enableProjectRules est false. Aucune regle disponible pour l'instant."
        },
        "linterSt.verboseOutput": {
          "type": "boolean",
          "default": false,
          "description": "Journalise dans Output > 'Linter ST' la commande complete lancee (python lint.py ...) et le JSON brut retourne, pour CHAQUE analyse (pas seulement en cas d'erreur de parsing). Utile pour comprendre precisement ce que fait le linter."
        },
        "linterSt.knownExternalTypes": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Types externes supplementaires (bibliotheques natives CODESYS/CANopen/EtherCAT, jamais declares dans CODE/) a classer en avertissement informatif plutot qu'en erreur -- ex: [\"DEVICE_STATE\", \"AXIS_REF\"]. Fusionne avec la liste deja connue dans lint.py (DEVICE_STATE). A completer au cas par cas quand un nouveau type externe legitime declenche une fausse erreur."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
